{% extends 'base.html.twig' %}

{% block title %}SpecialRules{% endblock %}

{% block body %}
    <h1>SpecialRules</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ special_rule.id }}</td>
            </tr>
            <tr>
                <th>Rule</th>
                <td>{{ special_rule.rule }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ special_rule.title }}</td>
            </tr>
            <tr>
                <th>SubRule</th>
                <td>{{ special_rule.subRule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('special_rules_index') }}">back to list</a>

    <a href="{{ path('special_rules_edit', {'id': special_rule.id}) }}">edit</a>

    {{ include('special_rules/_delete_form.html.twig') }}
{% endblock %}
