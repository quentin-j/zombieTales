<form method="post" action="{{ path('back_version_delete', {'id': version.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ version.id) }}">
    <button class="btn">Delete</button>
</form>
