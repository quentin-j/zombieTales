{% extends 'back/version/_base_back_version.html.twig' %}

{% block title %}Version{% endblock %}

{% block body %}
    <h1>Version</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ version.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ version.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_version_index') }}">back to list</a>

    <a href="{{ path('back_version_edit', {'id': version.id}) }}">edit</a>

    {{ include('back/version/_delete_form.html.twig') }}
{% endblock %}
